import { GitPluginPageData } from "@vuepress/plugin-git";
import { ThemeData } from "@vuepress/plugin-theme-data";
import { LocaleData, PageFrontmatter } from "vuepress/shared";

//#region src/shared/nav.d.ts
/** Base nav item, displayed as text */
interface NavItemOptions {
  text: string;
  ariaLabel?: string;
}
/** Options for `<AutoLink>` */
interface AutoLinkOptions extends NavItemOptions {
  /** Link of item */
  link: string;
  /** Rel of `<a>` tag */
  rel?: string;
  /** Target of `<a>` tag */
  target?: string;
  /** Regexp mode to be active */
  activeMatch?: string;
  /** Whether it's active only when exact match */
  exact?: boolean;
}
//#endregion
//#region src/shared/navbar.d.ts
/** Base nav group, has nav items children */
interface NavGroup<T> extends NavItemOptions {
  /** Link prefix of current group */
  prefix?: string;
  /** Link of current group */
  link?: string;
  /** Children of current group */
  children: T[];
}
type NavbarLinkOptions = AutoLinkOptions | string;
type NavbarGroupOptions = NavGroup<NavbarLinkOptions | NavGroup<NavbarLinkOptions>>;
type NavbarOptions = (NavbarGroupOptions | NavbarLinkOptions)[] | false;
//#endregion
//#region src/shared/sidebar.d.ts
type SidebarLinkOptions = AutoLinkOptions;
interface SidebarGroupOptions extends NavItemOptions {
  /** Link prefix of current group */
  prefix?: string;
  /** Link of current group */
  link?: string;
  /**
   * Whether current group is collapsible
   *
   * @default false
   */
  collapsible?: boolean;
  /** Children of current group */
  children: SidebarItemOptions[];
}
type SidebarItemOptions = SidebarGroupOptions | SidebarLinkOptions | string;
type SidebarArrayOptions = SidebarItemOptions[];
type SidebarObjectOptions = Record<string, SidebarArrayOptions | 'heading' | false>;
type SidebarOptions = SidebarArrayOptions | SidebarObjectOptions | 'heading';
//#endregion
//#region src/shared/options.d.ts
type DefaultThemeLocaleOptions = DefaultThemeData;
type DefaultThemeData = ThemeData<DefaultThemeLocaleData>;
interface DefaultThemeLocaleData extends LocaleData {
  /**
   * Default color mode
   *
   * @default 'auto'
   */
  colorMode?: 'auto' | 'dark' | 'light';
  /**
   * Enable color mode switching and display a button in navbar or not
   *
   * @default true
   */
  colorModeSwitch?: boolean;
  /**
   * Whether show external link icon
   *
   * @default true
   */
  externalLinkIcon?: boolean;
  /**
   * Home path of current locale
   *
   * Used as the link of back-to-home and navbar logo
   */
  home?: string;
  /**
   * Navbar config
   *
   * Set to `false` to disable navbar in current locale
   */
  navbar?: NavbarOptions;
  /** Navbar label used for screen readers using the `aria-label` attribute */
  navbarLabel?: string | null;
  /** Page navbar label used for screen readers using the `aria-label` attribute */
  pageNavbarLabel?: string | null;
  /**
   * Navbar logo config
   *
   * Logo to display in navbar
   */
  logo?: string | null;
  /**
   * Navbar logo config for dark mode
   *
   * Logo to display in navbar in dark mode
   */
  logoDark?: string | null;
  /**
   * The alt text of navbar logo. Defaults to the site title if not specified.
   * If the value is the same as the site title, the site title rendered in the
   * navbar will be hidden from screen readers to avoid duplication.
   */
  logoAlt?: string | null;
  /**
   * Navbar repository config
   *
   * Used for the repository link of navbar
   */
  repo?: string | null;
  /**
   * Navbar repository config
   *
   * Used for the repository text of navbar
   */
  repoLabel?: string;
  /**
   * Navbar language selection config
   *
   * Text of the language selection dropdown
   */
  selectLanguageText?: string;
  /**
   * Navbar language selection config
   *
   * Aria label of of the language selection dropdown
   */
  selectLanguageAriaLabel?: string;
  /**
   * Navbar language selection config
   *
   * Language name of current locale
   *
   * Displayed inside the language selection dropdown
   */
  selectLanguageName?: string;
  /**
   * Sidebar config
   *
   * Set to `false` to disable sidebar in current locale
   */
  sidebar?: SidebarOptions | false;
  /**
   * Sidebar depth
   *
   * - Set to `0` to disable all levels
   * - Set to `1` to include `<h2>`
   * - Set to `2` to include `<h2>` and `<h3>`
   * - ...
   *
   * The max value depends on which headers you have extracted via
   * `markdown.headers.level`.
   *
   * The default value of `markdown.headers.level` is `[2, 3]`, so the default
   * max value of `sidebarDepth` is `2`
   */
  sidebarDepth?: number;
  /**
   * Page meta - edit link config
   *
   * Whether to show "Edit this page" or not
   */
  editLink?: boolean;
  /**
   * Page meta - edit link config
   *
   * The text to replace the default "Edit this page"
   */
  editLinkText?: string;
  /**
   * Page meta - edit link config
   *
   * Pattern of edit link
   *
   * @example
   *   ':repo/edit/:branch/:path'
   */
  editLinkPattern?: string;
  /**
   * Page meta - edit link config
   *
   * Use `repo` config by default
   *
   * Set this config if your docs is placed in a different repo
   */
  docsRepo?: string;
  /**
   * Page meta - edit link config
   *
   * Set this config if the branch of your docs is not 'main'
   */
  docsBranch?: string;
  /**
   * Page meta - edit link config
   *
   * Set this config if your docs is placed in sub dir of your `docsRepo`
   */
  docsDir?: string;
  /**
   * Page meta - last updated config
   *
   * Whether to show "Last Updated" or not
   */
  lastUpdated?: boolean;
  /**
   * Page meta - last updated config
   *
   * The text to replace the default "Last Updated"
   */
  lastUpdatedText?: string;
  /**
   * Page meta - contributors config
   *
   * Whether to show "Contributors" or not
   */
  contributors?: boolean;
  /**
   * Page meta - contributors config
   *
   * The text to replace the default "Contributors"
   */
  contributorsText?: string;
  /**
   * Custom block config
   *
   * Default title of TIP custom block
   */
  tip?: string;
  /**
   * Custom block config
   *
   * Default title of WARNING custom block
   */
  warning?: string;
  /**
   * Custom block config
   *
   * Default title of DANGER custom block
   */
  danger?: string;
  /**
   * Custom block config
   *
   * Default title of IMPORTANT custom block
   */
  important?: string;
  /**
   * Custom block config
   *
   * Default title of NOTE custom block
   */
  note?: string;
  /**
   * 404 page config
   *
   * Not Found messages for 404 page
   */
  notFound?: string[];
  /**
   * 404 page config
   *
   * Text of back-to-home link in 404 page
   */
  backToHome?: string;
  /** A11y text for external link icon */
  openInNewWindow?: string;
  /** A11y text for color mode toggle button */
  toggleColorMode?: string;
  /** A11y text for sidebar toggle button */
  toggleSidebar?: string;
  /** Text for prev link */
  prev?: string | false;
  /** Text for next link */
  next?: string | false;
}
//#endregion
//#region src/shared/page.d.ts
interface DefaultThemePageData extends Partial<GitPluginPageData> {
  filePathRelative: string | null;
}
interface DefaultThemePageFrontmatter extends PageFrontmatter {
  home?: boolean;
  navbar?: boolean;
  pageClass?: string;
  externalLinkIcon?: boolean;
}
interface DefaultThemeHomePageFrontmatter extends DefaultThemePageFrontmatter {
  home: true;
  heroImage?: string;
  heroImageDark?: string;
  heroAlt?: string;
  heroHeight?: number;
  heroText?: string | null;
  tagline?: string | null;
  actions?: {
    text: string;
    link: string;
    type?: 'primary' | 'secondary';
  }[];
  features?: {
    title: string;
    details: string;
  }[];
  footer?: string;
  footerHtml?: boolean;
}
interface DefaultThemeNormalPageFrontmatter extends DefaultThemePageFrontmatter {
  home?: false;
  editLink?: boolean;
  editLinkPattern?: string;
  lastUpdated?: boolean;
  contributors?: boolean;
  sidebar?: SidebarArrayOptions | 'heading' | false;
  sidebarDepth?: number;
  prev?: AutoLinkOptions | string;
  next?: AutoLinkOptions | string;
}
//#endregion
export { NavbarOptions as _, DefaultThemeData as a, SidebarArrayOptions as c, SidebarLinkOptions as d, SidebarObjectOptions as f, NavbarLinkOptions as g, NavbarGroupOptions as h, DefaultThemePageFrontmatter as i, SidebarGroupOptions as l, NavGroup as m, DefaultThemeNormalPageFrontmatter as n, DefaultThemeLocaleData as o, SidebarOptions as p, DefaultThemePageData as r, DefaultThemeLocaleOptions as s, DefaultThemeHomePageFrontmatter as t, SidebarItemOptions as u, AutoLinkOptions as v, NavItemOptions as y };
//# sourceMappingURL=index-BNjRb2Mr.d.ts.map